# Configuração para Produção (VPS)
# Este arquivo define as variáveis de ambiente para build de produção

# URL da API do backend
# Em produção com Nginx, usa path relativo para proxy reverso
VITE_API_BASE_URL=/api

# Abacate Pay não precisa de chave pública no frontend
# O checkout é feito via redirecionamento seguro
